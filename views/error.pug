<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title OOPS 404
        link(rel="shortcut icon", href="/images/favicon.ico", type="image/x-icon")
        link(rel="stylesheet", href="/css/result.css")
        link(rel="stylesheet", href="/css/error.css")
    body
        header
            .container
                .title
                    a.home(href="/")
                        span.content Movie
                        span.special Wiki
                .form-control
                    form(action="/search", method="GET")
                        .search-bar
                            input(type="text", name="searchQuery", id="searchTitle", placeholder="Search for Movies", required)
                            button(id="search")
                                img(src="/images/searchicon.png", alt="Search")
        .result-container
            h1 Oops!! Couldnt find what you are looking for
            h1 Please check the url if its valid
            h1 The url: #[span.problem #{url}] may have been deprecated!! :-(
            h1
                a(href="/") Return Home Here